# 连接网络

难度：6.7

## 使用iwd连接网络

在Arch Linux ISO启动后的CLI中输入`iwctl`,会进入它的shell界面。

    [iwd]# _

键入`station wlan0 get-networks`后，获取网络信息。接着，键入`station wlan0 connect SSID`后输入密码，随后按`Ctrl-D`退出

## 其他可行的方法

一般其他可行的方法为自动获取（DHCP，需要`eth0`设备）型或需使用`mmctl`型，我就不演示了。