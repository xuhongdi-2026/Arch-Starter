## 本地化

编辑`/etc/locale.gen`，在适当位置去除`locale-gen`忽略的“#”号：

    ....
    zh_CN.UTF-8
    en_US.UTF-8
    ....

执行`locale-gen`后，编辑`/etc/locale.conf`：

    # LANG=zh_CN.UTF-8 # 这个在tty下会乱码的
    LANG=en_US.UTF-8

也可以使用`localectl`编辑locale：

    localectl set-locale LANG=en_US.UTF-8

